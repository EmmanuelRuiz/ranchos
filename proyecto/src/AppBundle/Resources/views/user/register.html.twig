{% extends "AppBundle:Layouts:layout.html.twig" %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('assets/js/validar-email.js') }}"></script>
{% endblock %}

{% block content %}

    <section class="row about_medicalpro_row">
        <div class="container">
            <div class="row titleRow">
                <h5>Crea una nueva cuenta</h5>
                <h2>Crear cuenta</h2>
            </div>
            <div class="row">
                <div class="container">
                    <div class="formulario-registro col-lg-4">
                        <div>
                            {{form_start(form, {'action':'', 'method':'POST'})}}
                            {{form_errors(form)}}
                            {{form_end(form)}}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
